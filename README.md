#this project allows you to add new Github users and sort them according to Name, Location and no of followers.
# To Run the project user npm install to install all the dependencies of the project.
# Run npm start command in terminal to run the project.
